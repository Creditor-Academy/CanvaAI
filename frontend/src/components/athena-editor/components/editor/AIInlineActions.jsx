import React from 'react';

export const AIInlineActions = ({ open, onOpenChange }) => {
  if (!open) return null;
  return (
    <div style={{ display: 'none' }}>
      AI Inline Actions Placeholder
    </div>
  );
};
