// slideFamilies/types.js
// JSDoc contract placeholders — no runtime code.

/**
 * @typedef {Object} SlideFamilyDefinition
 * @property {string} id - Canonical family id (e.g. "Hero", "Minimal")
 * @property {string} [description] - Human-readable purpose
 * @property {string[]} [preferredTemplates] - Layout template names compatible with this family
 * @property {(slide: Object, meta: Object) => boolean} [match] - Optional matcher for strategy routing
 */

export {};
